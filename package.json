{
  "name": "glowco-ember",
  "version": "1.0.0",
  "private": true,
  "description": "Ember — AI-native CRM for GlowCo skincare brand",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix stub && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:stub": "npm run dev --prefix stub",
    "dev:client": "npm run dev --prefix client"
  }
}
